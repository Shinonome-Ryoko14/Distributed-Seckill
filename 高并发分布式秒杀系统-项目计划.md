# 高并发分布式秒杀系统 —— 单人 8 周执行计划

> 定位：**业务很小，但围绕「高并发、缓存、消息、分布式一致性、可观测性、CI/CD」把现代后端工程完整走一遍**。
> 前提：第二个个人项目、单人、2 个月（8 周）、必须可落地且能讲深。
> 阅读顺序：本文为执行计划；`harness_project.md` 为 AI 助手协作守则（构建代码时先读它）。

---

## 0. 本项目想解决的问题

一个商品限量 N 件，开抢瞬间涌入海量请求：

```
10万人 → 网关 → 秒杀服务 → MySQL → UPDATE stock
                              ↓
              连接池耗尽 / CPU 飙升 / 超卖 / 雪崩
```

核心命题：**如何让一个只能扛几百 QPS 的传统订单系统，变成能扛瞬时高并发、且不超卖、一人一单、最终一致的系统？**

一句话主线：**把高并发流量挡在数据库之外，把订单创建从抢购请求里解耦出去。**

---

## 1. 范围边界（最重要的纪律）

### 1.1 最终形态（刻意收敛后）

```
5 个可执行程序（含网关），1 个仓库，1 台机器，docker-compose 一键启动
```

不是文档初稿里的 6 服务全拆，而是：

```
        Internet
           ↓
      [ Gateway ]          鉴权 / 限流 / trace_id / 路由
           ↓
   ┌───────┼────────┐
   ↓       ↓        ↓
 [User]  [Product] [Seckill]──Redis(Lua 原子门+一人一单)──成功
   │       │        │                                   ↓
 MySQL   MySQL    (只读缓存/预热)                    [Kafka: seckill-order]
                                                          ↓
                                                  [ Order ]──事务扣真实库存+建单
                                                    │        (幂等/唯一约束/DLQ)
                                                    ↓
                                                  MySQL + Redis ZSET 超时取消回补
```

### 1.2 服务与职责（5 进程）

| 服务 | 职责 | 复杂度 |
|---|---|---|
| gateway | 路由、JWT 校验、Redis+Lua 限流、trace_id、统一响应/错误 | 中 |
| user | 注册/登录/用户信息 | 低（薄） |
| product | 商品、秒杀活动主数据、缓存三兄弟（穿透/击穿/雪崩） | 中 |
| seckill | 核心：活动校验、Redis 预热、**Lua 原子扣库存 + 一人一单**、发 Kafka | 高 |
| order | 核心：消费 Kafka **幂等建单**、事务扣真实库存、订单状态机、超时取消、库存回补、DLQ | 高 |

### 1.3 刻意不做的清单（本轮不做，能说服自己为什么就行）

```
❌ Kubernetes / Service Mesh / Istio
❌ Nacos / Sentinel / Seata
❌ Elasticsearch / ClickHouse / MongoDB / Flink
❌ Loki + OpenTelemetry（第一版只上 Prometheus+Grafana 打 metrics，日志带 trace_id）
❌ gRPC 内部通信（服务间同步调用用 HTTP，解耦用 Kafka）
❌ Java/Spring Boot 复写（独立成"第二个项目"的素材，本轮不碰）
❌ 独立 Inventory 服务（库存写收敛到 order 一个服务，避免两套账）
❌ 多机房 / 分布式数据库 / 云原生全家桶
```

**原因一句话：单人 + 8 周，投入产出比太低的技术一律后置。**

---

## 2. 数据模型（单一事实源原则）

> 规则：**每张表只允许一个服务写入**。MySQL 是最终事实，Redis 只是闸门/缓存。

### 2.1 表清单（6 张，owner 标注）

| 表 | owner | 关键字段 | 说明 |
|---|---|---|---|
| `users` | user | id, username(uniq), password_hash, created_at | bcrypt |
| `products` | product | id, name, desc, price, status | 普通商品主数据 |
| `seckill_activities` | product | id, product_id, **seckill_stock**, seckill_price, start_time, end_time, status | **真实库存唯一事实源**，只有 order 的消费者写 stock 列 |
| `orders` | order | id, order_no(uniq), user_id, activity_id, price, status, created_at | **UNIQUE(user_id, activity_id)** → DB 级一人一单兜底 |
| `order_events` | order | event_id(PK), order_no, status, created_at | 消费去重兜底 |
| `inventory_op_logs` | order | id, activity_id, delta, order_no, type(扣/回补), created_at | 对账审计 |

> 唯一索引是幂等的最后防线：`order_events.event_id` PK + `orders` 的 UNIQUE 联合索引，**不信任"只靠消息语义恰好一次"**。

### 2.2 Redis Key 设计（约 10 个）

| Key | 类型 | 用途 |
|---|---|---|
| `product:detail:{id}` | string | 商品详情缓存（击穿：逻辑过期/singleflight；穿透：缓存空值） |
| `activity:info:{id}` | string | 秒杀活动信息缓存 |
| `seckill:stock:{activity_id}` | string | 预热后的原子库存（闸门值，从 DB 装载） |
| `seckill:users:{activity_id}` | set | 已抢到用户集合（Lua 内查重 → 一人一单第一道） |
| `seckill:hot:{activity_id}` | string | 活动是否预热完成标记 |
| `order:cancel:delay` | zset | 超时订单延时队列（member=order_no, score=应取消时间） |
| `ratelimit:{scope}:{key}` | string | 网关限流（token bucket，Redis+Lua） |
| `blacklist:{user_id}` | string | 网关黑名单/风控（可后置） |

> Redis 只做：闸门计数 + 热数据缓存。**库存真实值以 `seckill_activities.seckill_stock` 为准**，Redis 值与 DB 的偏差靠"预热刷新 + 回补通知 + 事后对账任务"收敛（最终一致），不要求强一致。

### 2.3 Kafka 设计（2 个业务 Topic + 1 个 DLQ）

| Topic | 分区 | 生产者 | 消费者 | 消息 |
|---|---|---|---|---|
| `seckill-order` | 3~6 | seckill | order | 抢购成功事件 |
| `seckill-order-dlq` | 1 | order | 人工/脚本回放 | 消费失败的死信 |

```json
// seckill-order 消息（带快照，order 无需回查 product）
{
  "event_id": "01J...",          // UUID，幂等依据
  "request_id": "abc123",        // 透传 trace
  "user_id": 10001,
  "activity_id": 999,
  "product_id": 88,
  "seckill_price": 1999,
  "stock_snapshot": 500,         // 演示/调试用
  "timestamp": 1788000000
}
```

---

## 3. 里程碑与 8 周排期

> 三个里程碑走完 V0→V2→V3，每一阶段都留有"问题→方案→验证"，本身就是面试素材。

### 3.1 里程碑 A：单体全链路（W1–W3）✅ 验收=正确性

目标：**先解决正确性与业务，不做任何架构**。一个 main.go 跑通全部，方便后面对照。

| 周 | 任务 | 完成标准 |
|---|---|---|
| W1 | 仓库脚手架；Makefile；配置加载；迁移 SQL；注册/登录(JWT HS256)；用户/商品/活动基础接口 | 3 张表建好，`/login` 能拿 token |
| W2 | 商品详情缓存（Cache Aside + TTL + 穿透空值缓存）；秒杀接口 **DB 条件更新防超卖**（`UPDATE ... SET stock=stock-1 WHERE stock>0`）+ 订单唯一约束一人一单；订单同步直建 | 并发正确性测试：100 线程抢 100 库存不超卖 |
| W3 | 订单超时取消：**Redis ZSET 延时队列**轮询取消 + 库存回补；`/seckill` 与库存回补对账日志；v0 压测记 baseline | 超时订单自动取消、库存正确回补 |

**基线压测（必须留档）**：记录"直连 MySQL 同步落单"在 1k/5k 并发下的现象（连接池耗尽、QPS、P99、错误率）→ 这是后来简历里"从 X 提升到 Y"的起点数字。

### 3.2 里程碑 B：拆服务 + Kafka 异步（W4–W6）✅ 验收=削峰与幂等

| 周 | 任务 | 完成标准 |
|---|---|---|
| W4 | 拆 4 个服务 + gateway（HTTP 互调，每表单写者）；gateway 挂 JWT 校验 + trace_id 中间件 | 拆分后功能与 A 等价回归通过 |
| W5 | Redis 预热脚本；**Lua 原子门**（查重→查库存→DECR→SADD，一次原子）；秒杀成功后发 Kafka 替代同步建单 | Lua 压测：10w 请求下 0 超卖、0 重复 |
| W6 | order 消费者幂等建单（event_id + 唯一约束，事务内扣真实库存）；消费失败重试 + **DLQ**；gateway Redis+Lua 限流；集成测试 | 重复消息注入 2 次只建 1 单；失败消息进 DLQ 可回放 |

### 3.3 里程碑 C：工程化 + 压测收口（W7–W8）✅ 验收=报告可交付

| 周 | 任务 | 完成标准 |
|---|---|---|
| W7 | docker-compose 全家桶（5 服务 + mysql/redis/kafka + prometheus/grafana）；Gin prometheus 中间件出 metrics；k6 压测（1k→10k 递增）定位瓶颈并优化（连接池/参数/Kafka 消费并发）；**故障注入**（手动 kill redis/kafka 观察行为与恢复） | 压测"优化前后对比"数据齐；断中间件后系统能自愈或报错清晰 |
| W8 | GitHub Actions（lint + unit test + build）；性能报告 + README + 架构图；**简历素材库**；面试题自测清单；缓冲一周 | 单机真实有效吞吐/延迟数据落文档；简历三句话能讲清全链路 |

**诚实数字原则**：不虚标"10w QPS"。单机 WSL 上报**实测有效吞吐 + P99 + 优化前后倍数**，讲清"瓶颈从 MySQL → 网关 → Kafka 消费"的迁移过程即可。

---

## 4. API 清单（≤25，版本化）

| 方法 | 路径 | 服务 | 说明 |
|---|---|---|---|
| POST | /api/v1/users/register | user | 注册 |
| POST | /api/v1/users/login | user | 登录 → JWT |
| GET | /api/v1/products/{id} | product | 商品详情（带缓存策略） |
| GET | /api/v1/products | product | 列表 |
| POST | /api/v1/products | product | 建商品（admin） |
| POST | /api/v1/activities | product | 建秒杀活动（admin，含预热触发） |
| GET | /api/v1/activities/{id} | product | 活动信息 |
| POST | /api/v1/seckill/{activity_id} | seckill | **核心抢购** |
| GET | /api/v1/orders | order | 我的订单 |
| GET | /api/v1/orders/{order_no} | order | 订单详情 |
| POST | /api/v1/orders/{order_no}/pay | order | 模拟支付（状态机前进） |
| POST | /api/v1/orders/{order_no}/cancel | order | 手动取消 |
| GET | /healthz | 各服务 | 健康检查（compose 用） |

网关限流默认全局限流 key；黑名单预留中间件位。

---

## 5. 工程化骨架（W1 就位，后续填充）

```
distributed-seckill/
├── services/
│   ├── gateway/          # cmd/  +  internal/
│   ├── user/
│   ├── product/
│   ├── seckill/
│   └── order/
├── internal/             # 各服务共享：response/err/jwt/redis/kafka/config/log(middleware)
├── migrations/           # 顺序编号 .sql
├── scripts/              # seed.go / warmup.go / reconcile.go
├── deploy/
│   └── docker-compose.yml
├── k6/                   # 压测脚本 + 结果记录
├── docs/                 # 架构图(ascii/mermaid)、故障演练记录、性能报告
├── Makefile
├── go.mod                # go 1.22+，共享 internal 走同一 module
└── .github/workflows/ci.yml
```

服务端口约定：gateway `:8080`、user `:8081`、product `:8082`、seckill `:8083`、order `:8084`。

---

## 6. 面试叙事主线（每阶段都要能讲"为什么"）

```
V0  直连 MySQL 同步落单      → 1k 并发就连接池耗尽（数据）
V1  Redis Lua 原子门 + 一人一单 → 把流量挡在 DB 外，吞吐量级提升（数据）
V2  Kafka 异步削峰 + 幂等     → 抢购与建单解耦，扛住瞬时洪峰（数据）
V3  网关限流 + 可观测 + 压测优化 → P99 / consumer lag 可解释（数据）
一致性故事：Lua 挡重放(一人一单) vs 唯一约束挡 MQ 重复(幂等) vs 超时回补(最终一致)
```

面试常考自测点：Redis 三兄弟怎么在商品/库存落地、为什么 Redis 不算真正的分布式锁来源、Lua 为什么原子、Kafka at-least-once 与幂等、为什么不选强一致方案、网关限流算法。

---

## 7. 风险与对策

| 风险 | 对策 |
|---|---|
| 想一次到位，2 个月做不完 | 严格按里程碑；里程碑内不追加需求；超期宁可砍 C 的边角也不压缩 B |
| 两套库存把自己绕晕 | 铁律：DB `seckill_activities.stock` 是唯一事实源，Redis 只是预热闸门 |
| 压测虚高，被面试追问击穿 | 只报实测 + 写明环境（单机/WSL/复用中间件），强调相对提升倍数 |
| AI 助手越权写超范围代码 | 遵循 `harness_project.md` 守则：一次一个模块、里程碑内做事 |
| 学到一半想换 Java/Spring | 本轮禁止；Go 学的是后端工程底层，与语言无关 |

---

## 8. 技术栈（项目最终清单）

| 层次 | 技术 | 掌握程度目标 |
|---|---|---|
| 语言 | **Go (1.22+)** | 熟练 |
| Web | **Gin** | 熟练 |
| 鉴权 | JWT (HS256) | 掌握 |
| 数据库 | **MySQL 8** | 深入（事务/索引/EXPLAIN/MVCC） |
| ORM/访问 | GORM（或 database/sql，视需要） | 熟练 |
| 缓存 | **Redis**（Lua 脚本原子扣减/一人一单/分布式锁/缓存三兄弟） | 深入 |
| 消息队列 | **Kafka**（producer/consumer/group/幂等/重试/DLQ/lag） | 深入 |
| 网关 | 自研 Gateway（路由/限流/鉴权/trace_id） | 理解并开发 |
| 容器 | **Docker / Docker Compose** | 熟练 |
| 可观测 | **Prometheus + Grafana**（metrics）；结构化日志带 trace_id | 掌握 |
| CI/CD | **GitHub Actions**（lint + unit test + build） | 能独立配置 |
| 测试 | go test + 集成测试（Redis/Kafka 可 testcontainer 或 docker 起依赖） | 掌握 |
| 压测 | **k6 / wrk** | 能做并出报告 |
| 版本控制 | Git（feature 分支 + PR） | 熟练 |
| 辅助开发 | AI Coding Agent（见 harness_project.md） | 熟练 |

> 刻意未列：gRPC、Loki、OpenTelemetry、K8s、Nacos、Seata、Spring 全家桶 —— 理由见 §1.3。它们是"下个项目/有余力再做"清单。

---

## 9. 结束态交付物（W8 验收单）

- [ ] 代码：5 服务可 docker-compose 一键起、功能全绿
- [ ] 测试：核心模块单测 + 关键集成测试（幂等/不超卖）
- [ ] CI：push 自动 lint/test/build
- [ ] 压测报告：优化前后 QPS/P99/错误率表格 + 瓶颈分析
- [ ] 故障演练记录：kill redis/kafka 后系统行为与恢复
- [ ] README + 架构图 + 启动文档
- [ ] 简历素材：三句话项目描述 + 4 条核心亮点（含真实数字）
